'
'		Serial I/O via BASIC.
'
buffer = alloc(2)
uconfig 115200
print "Waiting...."
repeat
	if event(t1,50) then n = (n + 1) & $3F:usend n+32
	if uhasdata()
		ureceive buffer,1
		if peek(buffer) >= 32:print chr$(peek(buffer));:else:print:endif
	endif
until false
next
end
