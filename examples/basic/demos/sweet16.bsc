'
'	Sweet 16 test program.
'
mem = alloc(512):reg = alloc(32):stack = alloc(16)
print mem
for pass = 0 to 1
	p = mem
	o = pass * 3

	set 2,4
	bsl  subr
	rtn

.subr	
	dcr 2
	rs
next

reg[15] = mem:reg[12] = stack
sweet reg
